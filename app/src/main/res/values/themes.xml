<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ToDoListApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/purple</item>
        <item name="colorPrimaryVariant">@color/purple</item>
        <item name="android:statusBarColor">@color/purple</item>
    </style>

    <style name="Theme.ToDoListApp" parent="Base.Theme.ToDoListApp" />

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/purple</item>
        <item name="colorPrimaryVariant">@color/purple</item>
        <item name="android:statusBarColor">@color/purple</item>
    </style>
    <style name="TimePickerTheme" parent="@style/ThemeOverlay.AppCompat.Dialog.Alert">
        <item name="android:textColorPrimary">@color/purple</item>
        <item name="android:textColorSecondary">@color/purple</item>
        <item name="colorControlActivated">#7E18B1</item>
        <item name="android:colorAccent">@color/purple</item>

        <item name="android:buttonBarPositiveButtonStyle">@style/DialogButtonStyle</item> <!-- تعيين استايل الزر الإيجابي -->
        <item name="android:buttonBarNegativeButtonStyle">@style/DialogButtonStyle</item> <!-- تعيين استايل الزر السلبي -->
    </style>
    <style name="ThemeOverlay.MaterialComponents.MaterialAlertDialog.Picker.Date.Header" parent="Widget.AppCompat.ActionBar.TabText">
        <item name="android:textColor">@color/purple</item>
    </style>
    <style name="DialogButtonStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/purple</item> <!-- تعيين لون النص للأزرار -->
    </style>
</resources>